# ------------------------------------- APP CONFIG --------------------------------------
PORT=8001

# ------------------------------------ TOKEN CONFIG -------------------------------------

# Must be at least 16 characters and not a common word. Generate with:
#   openssl rand -hex 32
# These are local-development values only - use fresh secrets in production.
JWT_SECRET=dev_jwt_dfgdfghd5324sgdhGDSGJHSFDSF_change_for_prod

COOKIE_SECRET=dev_cookie_werfsvíGrgíetsdélkvdsfsb_change_for_prod

# ----------------------------------- DATABASE CONFIG -----------------------------------

# Prisma CLI (generate/migrate) reads these:
DATABASE_URL=mysql://root:@localhost:3305/navigator

# Prisma CLI (generate/migrate) reads these:
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_PORT=3305
DB_NAME=navigator